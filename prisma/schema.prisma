generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
}

model organization {
  id        Int      @id @default(autoincrement())
  name      String
  slug      String?
  ownerId   Int
  createdAt DateTime @default(now())
}

